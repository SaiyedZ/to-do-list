<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Arial", sans-serif;
        }

        body {
            background-color: #0d1117; 
            color: #c9d1d9; 
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        
        .main {
            width: 100%;
            max-width: 600px;
            background-color: #161b22; 
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
        }

        .to-do-list h2 {
            font-size: 2rem;
            color: #58a6ff;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid #30363d; 
            padding-bottom: 10px;
        }

        

        .form {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        input[type="text"] {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #30363d; 
            border-radius: 8px;
            font-size: 1rem;
            color: #c9d1d9;
            background-color: #0d1117; 
            transition: border 0.3s ease;
        }

        input[type="text"]:focus {
            border-color: #58a6ff;
            background-color: #161b22;
        }

        button {
            padding: 12px 30px;
            font-size: 1rem;
            color: #fff;
            background-color:#1e88e5; 
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        button:hover {
            background-color:#1e88e5; 
            transform: scale(1.05); 
        }

        button:focus {
            outline: none;
            box-shadow: 0 0 0 3px #1e88e5(34, 197, 94, 0.5); 
        }

        @media (max-width: 768px) {
            .main {
                padding: 20px;
            }

            .form {
                flex-direction: column;
            }

            input[type="text"] {
                margin-bottom: 15px;
            }

            button {
                width: 100%;
            }
        }

    </style>
</head>

<body>
    <div class="main">
        <div class="to-do-list">
            <h2>To-Do List</h2>
            <form action="/insertdata" method="post">
                <div class="form">
                    <input type="text" name="task" placeholder="add task" value="<%= data.task %>">
                    <input type="hidden" name="editid" placeholder="No." value="<%= data.id %>">
                    <button type="submit">edit</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>
